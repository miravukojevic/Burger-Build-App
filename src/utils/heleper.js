export const addNumbers = (a, b) => a + b;
export const substractNumbers = (a, b) => a - b;
const multiNumbers = (a,b) => a*b;

const myFunctions = {
    addNumbers,
    substractNumbers,
    multiNumbers
}





export default myFunctions;


